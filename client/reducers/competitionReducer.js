'use strict'
import {
  ACTION_CONTENT_SWITCH,
  ACTION_COMPETITION_SWITCH
} from '../constants/actionType'

export function contentReducer(state = { competitionId: '0' }, action) {
  // switch (action.type) {
  //   case ACTION_COMPETITION_SWITCH:
  //     return { ...state, ...action.payload }
  // }

  return state
}
